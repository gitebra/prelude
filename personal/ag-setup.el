;;; ag-setup.el --- Ag settings
;;; Commentary:
;;; Code:

(message "Setup ag...")
(require 'ag)
(setq ag-highlight-search t)
(setq ag-reuse-buffers 't)

(provide 'ag-setup)
;;; ag-setup.el ends here
