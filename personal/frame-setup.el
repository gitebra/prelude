;;; frame-setup.el --- Set up frame parameters.
;;; Commentary:
;;; Code:

(add-to-list 'default-frame-alist '(width  . 132))
(add-to-list 'default-frame-alist '(height . 60))

(provide 'frame-setup)
;;; frame-setup.el ends here
