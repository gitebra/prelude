;;; undo-tree-setup.el --- Undo tree settings
;;; Commentary:
;;; Code:

;; Use undo tree
(require 'undo-tree)

;; Use undo-tree-mode by default in all buffers.
(global-undo-tree-mode)

(provide 'undo-tree-setup)
;;; undo-tree-setup.el ends here
